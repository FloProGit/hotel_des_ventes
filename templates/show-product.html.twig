<div class="container flex justify-center">
    <div class="columns-2">
        <div>
            <img class="w-full" src="https://picsum.photos/300/300" alt="{{name}}">
            <button class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded-full">Ajouter au panier</button>
        </div>
        <div class="px-6 py-4">
            <div class="font-bold text-xl mb-2 text-end">{{ name }}</div>
            <p class="text-gray-700 text-base">
            {{ description }}
            </p>
            <div class="px-6 pt-4 pb-2">
                <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mx-auto mb-2">#category</span>
            </div>
            <p class="text-green-600 success text-end">{{ price_excl_taxe * 1.2 }} € TTC</p>
        </div>
    </div>
</div>
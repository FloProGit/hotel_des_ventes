<div class="max-w-sm rounded overflow-hidden shadow-lg mb-4 mx-2">
	<a href="product/{{product.id}}">
		<img class="w-full" src="https://picsum.photos/300/300" alt="{{product.name}}">
		<div class="px-6 py-4">
			<div class="font-bold text-xl mb-2 text-center">{{ product.name }}</div>
			<p class="text-gray-700 text-base">
				{{ product.description }}
			</p>
			<p class="text-green-600 success text-end">{{ product.priceexcltaxe }}
				€ HT</p>
		</div>
		<div class="px-6 pt-4 pb-2">
			<span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mx-auto mb-2">#category</span>
		</div>
	</a>
</div>
